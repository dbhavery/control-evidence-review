# Centralized logging configuration (synthetic)
# Ships logs via syslog to the log aggregation cluster.
destination centralized_logging { tcp("10.0.0.5" port(514)); };
api_key=sandbox-demo-not-a-real-key-0001
